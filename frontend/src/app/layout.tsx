import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Mentoria IA 2025 - RAG Search',
  description: 'Intelligent document search using RAG (Retrieval-Augmented Generation) with hybrid search capabilities',
  keywords: 'RAG, AI, search, documents, artificial intelligence',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="pt-BR">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
